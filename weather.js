#!/usr/bin/env node
import { parseArgv } from './helpers/argv.js'

const initCLI = () => {
  const { h, s, t } = parseArgv(process.argv.slice(2))

  if (h) {
  }

  if (s) {
  }

  if (t) {
  }
}

initCLI()
